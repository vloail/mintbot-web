name: fetch-archetype-src
on:
  workflow_dispatch:
  push:
    paths: ['.github/workflows/fetch-archetype-src.yml']

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fetch Archetype Logic source
        run: |
          mkdir -p scatter_data
          curl -sL --max-time 90 "https://robinhoodchain.blockscout.com/api?module=contract&action=getsourcecode&address=0x5b6d1081687f94abc51d59fbb061bf894600e4cb" \
            -H "User-Agent: Mozilla/5.0" -o scatter_data/archetype_logic_source.json || true
          wc -c scatter_data/archetype_logic_source.json 2>/dev/null
      - name: Commit results
        run: |
          git config user.name "vloail"
          git config user.email "vloail@users.noreply.github.com"
          git add scatter_data/ || true
          git commit -m "fetch: archetype logic source" || echo "no changes to commit"
          git push origin main || echo "push failed"
